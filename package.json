{
  "name": "reqheadparser",
  "version": "1.0.0",
  "description": "Small app to parse HTML request header",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "bararan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2"
  }
}
